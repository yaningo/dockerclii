version: 2.1
  
orbs:
  docker: circleci/docker@1.5.0
  
parameters:
  la_version:
   type: string
   description: Remote docker version to setup for docker builds.
   default: zero
  
  
jobs:
  setup:
    docker:
      - image: circleci/node
#     parameters:
#       docker-version:
#        type: env_var_name
#        description: Remote docker version to setup for docker builds.
#        default: DOCKER_VERSION
    steps:
      - run: echo << pipeline.parameters.la_version >>
      - docker/install-docker:
          version: v<< pipeline.parameters.la_version >>
      - setup_remote_docker:
          version: << pipeline.parameters.la_version >>
          
workflows:
  build-deploy:
    jobs:
      - setup
