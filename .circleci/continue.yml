version: 2.1

parameters:
  further:
    type: boolean
    default: true
  cucumber-options:
    type: string
    default: "--tags @check"

jobs:
  lejob:
    docker:
      - image: cimg/base:stable
    steps:
      - run: |
          echo << pipeline.parameters.further >>
          echo << pipeline.parameters.cucumber-options >>
          echo "Hello, I'm continuing"

workflows:
  setup-workflow:
#     when: << pipeline.parameters.further >>
    jobs:
      - lejob
