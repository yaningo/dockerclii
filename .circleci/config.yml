version: 2.1

parameters:
  who-org:
    type: string

#### This is the forkeeeee

jobs:
  premier:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job1
                   echo 'export pipeline.parameter.who-org=$CIRCLE_USERNAME' >> $BASH_ENV
      
  deuxieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job2
 
  troisieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo jobnum3
                                     
workflows:
  version: 2
  papa:
    when:
      equal: [ tech5irl, << pipeline.parameter.who-org >> ]
    jobs:
      - premier
          
  # papu:
  #  jobs:
  #    - premier
      
  tata:
    jobs:
      - deuxieme:
          context: Lecontext
  lala:
    jobs:
      - troisieme:
          context: Lecontext
