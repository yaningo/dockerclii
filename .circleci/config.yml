version: 2
  
jobs:
  setup:
    docker:
      - image: yaningo/my-node:test
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
        environment:
          ENVFROMCONTEXT: "$toto_var"
    steps:
      - run: echo $ENVFROMCONTEXT

#       - checkout
#       - setup_remote_docker
#           version: 19.03.13
#       - run: docker version
#       - run:
#           name: Build docker image
#           command: |
#             make build/image
      
workflows:
  version: 2
  build-deploy:
    jobs:
      - setup:
          context:
            - toto
