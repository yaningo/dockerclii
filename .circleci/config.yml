version: 2.1

jobs:
  setup:
    docker:
      - image: circleci/python
      
    environment:
      FOO: bar
      
    steps:
      - setup_remote_docker:
          version: 19.03.13
          
      - run: |
          ssh remote-docker \<<EOF
            export FOO
            sudo bash -c 'echo $FOO'
            echo $FOO
            sudo docker info
          EOF
          
          
workflows:
  build-deploy:
    jobs:
     - setup
      
