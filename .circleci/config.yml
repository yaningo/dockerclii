version: 2
  
jobs:
  setup:
    machine:
      image: ubuntu-2004:202107-02
          
    steps:
      - run: docker run --env ENVFROMCONTEXT=$toto_var yaningo/my-node:test env

#       - checkout
#       - setup_remote_docker
#           version: 19.03.13
      - run: docker version
#       - run:
#           name: Build docker image
#           command: |
#             make build/image
      
workflows:
  version: 2
  build-deploy:
    jobs:
      - setup:
          context:
            - toto
