version: 2.1
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
#       - checkout
      - run: echo "Public IP is now $(curl checkip.amazonaws.com)"
      
      - setup_remote_docker:
           version: 20.10.7
           
      - run: echo "Public IP is now $(curl checkip.amazonaws.com)"

      - run: |
          ssh remote-docker \ ssh -V
      - run: |
          ssh remote-docker \ curl checkip.amazonaws.com
          
workflows:
  build-deploy:
    jobs:
      - setup
