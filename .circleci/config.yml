version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:edge #public.ecr.aws/eks-distro/kubernetes/pause:3.6
#       - image: postgres:12.2-alpine
#         environment:
#           POSTGRES_PASSWORD=postgres

    steps:
      - setup_remote_docker:
          version: 20.10.6
#       - checkout
      - run: docker run --name psql-server -d cimg/postgres:15.2 -e POSTGRES_PASSWORD=postgres
#       - run: dockerize -wait tcp://172.17.0.3:5432 -timeout 1m
      - run: docker network ls
      - run: docker network inspect bridge
      - run: docker network inspect ZZZL1Z3UP7J

workflows:
  workflow1:
    jobs:
      - build
