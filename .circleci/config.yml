version: 2
  
jobs:
  setup:
    machine:
      image: ubuntu-2004:202010-01
          
    steps:
      - run: docker run --rm yaningo/my-node:test --e $toto_var echo $toto_var

#       - checkout
#       - setup_remote_docker
#           version: 19.03.13
#       - run: docker version
#       - run:
#           name: Build docker image
#           command: |
#             make build/image
      
workflows:
  version: 2
  build-deploy:
    jobs:
      - setup:
          context:
            - toto
