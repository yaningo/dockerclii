version: 2.1
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
#       - checkout
      - run: |
          sudo echo 'alias liststuff="ls -al"' >> ~/.profile
          source ~/.profile
          liststuff
      
      - run: liststuff
          
workflows:
  build-deploy:
    jobs:
      - setup
