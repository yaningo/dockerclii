version: 2
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.2
      - run: docker version
      - run:
          name: Build docker image
          command: |
            make build/image
      
workflows:
  version: 2
  build-deploy:
    jobs:
      - setup
