version: 2.1

orbs:
  browser-tools: circleci/browser-tools@1.2.5
jobs:

  test:
    machine:
      image: ubuntu-2004:202010-01
    resource_class: small

    steps:
      - checkout
      - run: sudo ls -al ~/.ssh
            
workflows:
  push:
    jobs:
      - test
