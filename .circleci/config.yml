version: 2.1
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
#       - checkout
      - run: echo 'alias liststuff="ls -al"' >> $BASH_ENV
           
      - run: liststuff
          
workflows:
  build-deploy:
    jobs:
      - setup
