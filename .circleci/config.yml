version: 2.1

jobs:
  travail:
    working_directory: ~/project/wksp
    docker:
      - image: cimg/node:10.24.0
    steps:
      - checkout
      
      - run: |
          ls -al wksp
          pwd
          echo "toto" > new-file.txt
      - persist_to_workspace:
          root: .
          paths:
            - new-file.txt
      
  leworkspace:
    working_directory: ~/project/wksp
    docker:
      - image: cimg/node:10.24.0
    steps:
      - checkout
      - run: |
          pwd
          ls -al
      - attach_workspace:
          at: ~/project/wksp
      - run: |
          pwd
          ls -al
                   
workflows:

  whodidthis:
    jobs:
      - travail
      - leworkspace:
          requires:
            - travail
