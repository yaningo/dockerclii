version: 2.1

jobs:
  master-build:
    docker:
#       - image: node:14.18.2-alpine
      - image: cimg/node:17.6.0
    steps:
      - run: hash
      - run: hash npm
      - run: hash
      - run: type -a npm

workflows:
  ci:
    jobs:
      - master-build
