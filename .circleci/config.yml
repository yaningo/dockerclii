version: 2.1

jobs:
  master-build:
    docker:
#       - image: node:14.18.2-alpine
      - image: cimg/node:17.6.0
    steps:
      - run: hash npm
      - run: hash -t
      - run: type -a npm
#       - run: apk --no-cache add bash
#       - run: bash -c 'hash npm'
#       - run: bash -c 'hash'

workflows:
  ci:
    jobs:
      - master-build
