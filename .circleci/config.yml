version: 2.1

jobs:
  master-build:
    docker:
      - image: node:14.18.2-alpine
    steps:
      - run: apk --no-cache add bash
      - run: hash | grep npm

workflows:
  ci:
    jobs:
      - master-build
