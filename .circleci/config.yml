version: 2.1

#### This is the fork

jobs:
#  premier:
#    docker:
      - image: circleci/node
    
     steps:
      - run: echo $CIRCLE_USERNAME
      
      - when:
          condition:
            equal: [ yaningo, $CIRCLE_USERNAME ]
          steps:
             - run:
                 command: |
                       echo job1
                       
                   
  deuxieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job2
 
  troisieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo jobnum3
                                     
workflows:
  version: 2
  papa: 
    jobs:
      - premier
          
  # papu:
  #  jobs:
  #    - premier
      
  tata:
    jobs:
      - deuxieme:
          context: Lecontext
  lala:
    jobs:
      - troisieme:
          context: Lecontext
