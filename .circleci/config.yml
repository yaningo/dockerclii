version: 2.1

#### This is the forkeeeee

jobs:

  premier:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job1
                   sleep 2
      
  deuxieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job2
                   echo ${WHO_APPROVED_ID}
                   echo ${WHO_APPROVED_USERNAME}
                   echo $ACCESS_GRANTED
                   
                                      
 
  troisieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo jobnum3
                                     
workflows:
  version: 2
  papa:
    when:
      equal: [ "https://github.com/yaningo/dockercli", << pipeline.project.git_url >> ]
     # equal: [ "tech5irl", ${CIRCLE_USERNAME} ]
    jobs:
      - premier:
          pre-steps:
            - run:
                command: |
                  WF_NAME=$(curl --header "Circle-Token: $MyToken" --request GET "https://circleci.com/api/v2/workflow/${CIRCLE_WORKFLOW_ID}"|jq -r .name)
                  
                  echo $WF_NAME
                  
                  CURRENT_PIPE="<< pipeline.id >>"
                  echo $CURRENT_PIPE

                  ## Get the IDs of pipelines created by the current user on the same branch
                  PIPE_IDS=$(curl --header "Circle-Token: $MyToken" --request GET "https://circleci.com/api/v2/project/gh/yaningo/$CIRCLE_PROJECT_REPONAME/pipeline?branch=$CIRCLE_BRANCH"|jq -r --arg CIRCLE_USERNAME "$CIRCLE_USERNAME" '.items[]|select(.state == "created")|select(.trigger.actor.login == $CIRCLE_USERNAME).id')

                  echo $PIPE_IDS

                  #IFS=" "

                  ## Get the IDs of curently running workflows with the same name
                  if [ ! -z "$PIPE_IDS" ]; then
                    for PIPE_ID in $PIPE_IDS
                    do
                      curl --header "Circle-Token: $MyToken" --request GET "https://circleci.com/api/v2/pipeline/${PIPE_ID}/workflow"|jq -r --arg CIRCLE_WORKFLOW_ID "$CIRCLE_WORKFLOW_ID" --arg WF_NAME "${WF_NAME}" '.items[]|select(.status == "on_hold")|select(.name == $WF_NAME)|select(.id != $CIRCLE_WORKFLOW_ID)|.id' > WF_to_cancel.txt
                    done
                  fi
                  
                  cat WF_to_cancel.txt

                  ## Cancel any curently running workflow with the same name
                  if [ -s WF_to_cancel.txt ]; then
                    while read WF_ID;
                    do
                      curl --header 'Circle-Token: $MyToken' --request POST https://circleci.com/api/v2/workflow/${WF_ID}/cancel
                    done < WF_to_cancel.txt
                  else
                    echo "Nothing to cancel"
                  fi
      - wait-for-approval:
          type: approval
          requires:
            - premier
      - deuxieme:
          pre-steps: # steps to run before steps defined in the job bar
              - run:
                  command: |
                    WHO_APPROVED_ID=$(curl -X GET https://circleci.com/api/v2/workflow/${CIRCLE_WORKFLOW_ID}/job -H "Circle-Token: ${MyToken}" | jq -r '.items[]|select(.type == "approval")|.approved_by')
                    WHO_APPROVED_USERNAME=$(curl -X GET https://circleci.com/api/v2/user/${WHO_APPROVED_ID} -H "Circle-Token: ${MyToken}" | jq -r .name)
                    ACCESS_GRANTED=false
                    for OUTPUT in $(curl -u"${BB_admin_username}:${BB_admin_App_Password}" --request GET https://api.bitbucket.org/1.0/groups/yndmgo/${Approvers_Group}/members|jq -r '.[].display_name')
                    do
                      echo $OUTPUT
                      if [[ $OUTPUT = ${WHO_APPROVED_USERNAME} ]]; then
                        ACCESS_GRANTED=true
                      fi
                    done

                    if [[ $ACCESS_GRANTED=false ]]; then
                      curl -X POST https://circleci.com/api/v2/workflow/${CIRCLE_WORKFLOW_ID}/cancel -H "Circle-Token: ${MyToken}"
                    fi
                    echo ${WHO_APPROVED_ID}
                    echo ${WHO_APPROVED_USERNAME}
                    echo $ACCESS_GRANTED
          requires:
            - wait-for-approval
#   papu:
#    jobs:
#      - premier
  
#  tata:
#    jobs:
#      - deuxieme
      
#  tata:
#    jobs:
#      - deuxieme:
#          context: Lecontext
#  lala:
#    jobs:
#      - troisieme:
#          context: Lecontext
