version: 2.1

jobs:
  master-build:
    docker:
#       - image: node:14.18.2-alpine
      - image: cimg/node:17.6.0
    steps:
      - run: |
          hash
          hash -t npm
          hash
          type -a npm

workflows:
  ci:
    jobs:
      - master-build
