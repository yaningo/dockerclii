version: 2.1
jobs:
  build:
    docker:
      - image: redis:4

    steps:
      - run:
          name: Check buffer
          command: redis-cli config set client-output-buffer-limit
        
workflows:
  version: 2
  workflow:
    jobs:
      - build
