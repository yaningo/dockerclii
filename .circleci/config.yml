version: 2

jobs:
  premier:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job1
                   echo $HOME
                   
  deuxieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo job2
 
  troisieme:
    docker:
      - image: circleci/node
    steps:
      - run:
          command: |
                   echo jobnum3
                                     
workflows:
  version: 2
  papa:
    jobs:
      - premier
   
#       - start-trigger-metrics-collection:
#           type: approval

#       - deuxieme:
#           requires:
#             - start-trigger-metrics-collection
      
#   lala:
#     jobs:
#       - troisieme
