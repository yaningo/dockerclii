version: 2.1

executors:
  main:
    docker:
      - image: cimg/base:2022.01-18.04

jobs:
  build:
    executor: main
    steps:
      - run: |
          ssh -V

workflows:
  workflow1:
    jobs:
      - build
