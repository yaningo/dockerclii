version: 2.1
  
jobs:
  setup:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run:
          name: Docker version
          command: |
            docker --version
      
          
workflows:
  build-deploy:
    jobs:
      - setup
