version: 2.1

setup: true
 
orbs:
  continuation: circleci/continuation@0.1.2

jobs:
  init:
    executor: continuation/default
    steps:
      - checkout
      - run: echo $DOCKER_VERSION
      - continuation/continue:
          parameters: '{"la_version": "$DOCKER_VERSION"}'
          configuration_path: .circleci/local.yml
        
workflows:
  setup:
    jobs:
      - init
