version: 2.1

jobs:
  setup:
    docker:
      - image: circleci/python
      
    environment:
      FOO: bar
      
    steps:
      - setup_remote_docker:
          version: 19.03.13
          
      - run: |
          ssh remote-docker \<<EOF
            sudo bash -c 'echo $toto_var'
            echo $toto_var
          EOF
          
          
workflows:
  build-deploy:
    jobs:
     - setup
         contexts:
           - toto
      
