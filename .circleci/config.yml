# version: 2.1
  
# jobs:
#   setup:
#     docker:
#       - image: circleci/node
#     steps:
#       - checkout
#       - setup_remote_docker
# #           version: 20.10.7
#       - run: docker build .
          
# workflows:
#   build-deploy:
#     jobs:
#       - setup

version: 2.1

jobs:
  build:
    docker:
      - image: 717232957798.dkr.ecr.us-west-2.amazonaws.com/loom-base:4.1.0
    steps:

      - run:
          name: What SSH version?
          command: ssh -V
  
workflows:
  build-deploy:
    jobs:
      - build
