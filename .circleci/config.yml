version: 2.1
    
jobs:
  setup:
    machine: true
    steps:
      - run: |
          aws --version
# Comment here 😃           
                
workflows:
  build-deploy:
    jobs:
      - setup
