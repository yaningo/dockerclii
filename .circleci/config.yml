version: 2.1
    
jobs:
  setup:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run: |
          aws --version
# Comment here 😃           
                
workflows:
  build-deploy:
    jobs:
      - setup
