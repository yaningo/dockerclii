version: 2.1
    
jobs:
  le-job:
    machine:
      image: android:2022.12.
    working_directory: ~/gelato-os-flutter
    
    steps:
      - run: |
          which gradlew

workflows:
  my-workfw:
    jobs:
      - le-job
