version: 2.1
jobs:
  build:
    docker:
      - image: openjdk:8

    steps:
      - run:
          name: Install Docker client
          command: apk add docker-cli
          
          name: Find docker
          command:  which docker
           
      - checkout

workflows:
  version: 2.1
  build:
    jobs:
      - build