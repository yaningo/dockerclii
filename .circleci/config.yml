version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:edge #public.ecr.aws/eks-distro/kubernetes/pause:3.6
#       - image: postgres:12.2-alpine
#         environment:
#           POSTGRES_PASSWORD=postgres

    steps:
      - setup_remote_docker:
          version: 20.10.6
      - checkout
      - run: docker run --name psql-server -d postgres:12.2-alpine -e POSTGRES_PASSWORD=postgres -ip="172.17.0.3"
#       - run: dockerize -wait tcp://172.17.0.3:5432 -timeout 1m
      - run: docker network ls
      - run: docker network inspect bridge

workflows:
  workflow1:
    jobs:
      - build
