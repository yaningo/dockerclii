version: 2.1
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
#       - checkout
      - run: |
          sudo echo 'alias liststuff="ls -al"' > ~/.bash_aliases
          source ~/.bashrc
          cat ~/.bashrc
          cat ~/.profile
          cat ~/.bash_aliases
      
      - run: liststuff
          
workflows:
  build-deploy:
    jobs:
      - setup
