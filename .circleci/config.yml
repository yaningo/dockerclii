version: 2.1
  
jobs:
  setup:
    docker:
      - image: circleci/node
    steps:
      - run:
          name: I test I test
          command: |
              echo "export plan_diff=tototot" >> $BASH_ENV; source $BASH_ENV
              echo ' export MESSAGE="Changes to be applied from terraform: \`\`\`$plan_diff\`\`\`" ' >> $BASH_ENV
              source $BASH_ENV
              echo $MESSAGE
      
  setup-windows:
    executor:
      name: windows/default
      shell: bash.exe
    steps:
      - run: git version
          
workflows:
  build-deploy:
    jobs:
      - setup
#       - setup-windows
