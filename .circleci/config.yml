version: 2.1

executors:
  main:
    docker:
      - image: cimg/ruby:3.1

jobs:
  build:
    executor: main
    steps:
      - run: |
          which ruby

workflows:
  workflow1:
    jobs:
      - build
